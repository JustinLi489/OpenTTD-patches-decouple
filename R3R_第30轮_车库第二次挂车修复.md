# R3R 第 30 轮：车库「第二次挂车」修复（KI-68）

## 一句话

解挂出的车底段被提升为 front engine 后**没有重建车辆 tick 缓存**，导致它从此不再被 `Tick()`，`current_order` 永远停在 `OT_NOTHING`，机车侧挂接闸门永久以 `reject=target-not-wait` 拒绝 —— 这就是「库里第二次挂不上车」。

## 证据链（`build\R3R_debug.log`）

| 日志 | 含义 |
|---|---|
| `DECOUPLE-DONE u=8 co=1 real=0` | 解挂成功；段身份 ★ 保留、索引已钉到 `WAIT_COUPLE` |
| `U-ORD 0 = type 17` | 排程里 `WAIT_COUPLE` 在、索引也对 |
| `CPL-GATE reject=target-not-wait tgt=8 tOrd=0` | 但运行时 `current_order` 是 `OT_NOTHING`（从未装载） |
| u=8 解挂后**零日志行** | 无 `DEPOT-ARR` / 无 `SKIP-STOPPED` / 无任何订单处理 ⇒ 根本没进 `TrainLocoHandler` |

三者互相印证：**不是索引错、不是排程错、不是分组错，是这个段没有被 Tick。**

## 机制

`CallVehicleTicks()`（`vehicle.cpp`）只在 `_tick_caches_valid == false` 时才重建 `_tick_train_front_cache`；`Train::Tick()`（`train_cmd.cpp:10525`）里 `TrainLocoHandler` 只对 `IsFrontEngine()` 执行。

`DecoupleTrain` 把解出部分的链头提升为新 front engine（引擎分支 `u->SetFrontEngine()`、纯车厢分支 `R3RCreateCarOnlyFormation()`），这发生在**本 tick 缓存已建好之后**，却又没有 invalidate ⇒ 新 front 不在缓存里、缓存又不重建 ⇒ 永不被 Tick ⇒ `ProcessOrders` 不跑 ⇒ `current_order` 恒 `OT_NOTHING` ⇒ 闸门 `R3RCanCoupleNow` 永久拒绝。

旁证：车站那次解挂的 `u=3` 解挂前**已经是** front（本就在缓存中），所以它一切正常 —— 差异只在「提升前是否已在缓存里」。

这正是 `TryTrainCouple` 身份迁移分支（`head != v`）早就写了 `InvalidateVehicleTickCaches()` 并留了同款注释的原因，**解挂路径漏了这一处**。

## 落地改动

1. `src/train_cmd.cpp` — `DecoupleTrain` 身份处理末尾（`NormaliseTrainHead(u); NormaliseTrainHead(v);` 之后）新增：

```cpp
	/* R3R: the released part's head was just promoted to a front engine ...
	 * That cache is only rebuilt when it has been invalidated, therefore
	 * without this call the released part would never be Tick()ed again ...
	 * Mirrors the same fix at the identity relocation in TryTrainCouple(). */
	InvalidateVehicleTickCaches();
```

2. `src/vehicle_cmd.cpp` — 同族隐患一并加固：`R3RPromoteFreeWagonChainToFront()`（散链升段路径 `CmdMakeSegment`）末尾同样补 `InvalidateVehicleTickCaches();`。

**规则化**：凡「把非 front 车提升为 front engine」的路径（`DecoupleTrain` / `TryTrainCouple` 身份迁移 / `R3RPromoteFreeWagonChainToFront`）都必须紧跟一次 `InvalidateVehicleTickCaches()`。

## 闭合性核对

`train_cmd.cpp:4442` 解挂时已 `u->vehstatus.Reset(VehState::Stopped)` ⇒ u=8 一旦被 Tick 就不会被 `:10001` 的 `SKIP-STOPPED`（`Stopped && cur_speed == 0`）挡住，会走到 depot 块、进 `ProcessOrders` 装载 `WAIT_COUPLE`，随后停在原地等挂。

## 验证

- 增量编译 `EXITCODE=0`：`build\R3R_incbuild.log` = `[2/4] vehicle_cmd.cpp.obj` → `[3/4] train_cmd.cpp.obj` → `[4/4] Linking CXX executable openttd.exe`
- 仅改两个 `.cpp`，未碰 `src/*.h` ⇒ 不触发 KI-15（混合对象）/ KI-16（语言包版本）
- `build\openttd.exe` @ 21:06:19 晚于两个源码；lint 0 诊断
- 已按长期规则登记 **KI-68**，并更新 `R3R_KNOWN_ISSUES.md` 头部「最近一轮 = 第 30 轮」

## 请这样复测

库内两次挂车场景：

1. 解挂后**车底段自己应该开始刷处理行**（`DEPOT-ARR` / `SKIP-STOPPED` 等）；
2. 它的 `curType` 应从 0 变成 **17**（`WAIT_COUPLE`）；
3. 机车回到库里后应见 `CPL-GATE` **通过** 并打出 `COUPLE-OK`。

若仍有卡点，把新日志发来 —— `reject=` 串会直接指出是哪一条条件没过。
