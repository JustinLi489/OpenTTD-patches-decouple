# R3R artic 组"假爸爸身份"换向设想备忘

日期:2026-09-05
状态:用户新想法,仅解析存档,未实现、未定夺
相关:idx8↔idx3 断口(FLIP-U 就地镜像 artic 编组)分析见 R3R_couple_fold_catchup.md / R3R_artic_couple_memo.md

## 1. 用户想法(原文要点)

"把链头定义为前方向,那么一个铰接式内爸爸车厢(父车)必须在儿子车厢(artic parts)的前面。
能不能给铰接式内最尾部的车厢赋予一个『假爸爸身份』:
在 first/last 反转时,假爸爸变成新爸爸车厢,原来的爸爸退化为假爸爸身份;
而铰接式列车各部分的图像不变。"

即:用"爸爸身份在 artic 组两端间迁移"代替"artic 组物理翻转/就地镜像",
希望既达到逻辑换向效果,又不破坏 artic 组图像与物理姿态。

## 2. 概念映射(先对位代码)

- 物理爸爸(真父车):artic 组头。artic 块 = 父车 + 其后连续 artic parts,
  由 AddArticulatedParts(articulated_vehicles.cpp ~380)SetNext 焊死生成,
  父车先于儿子,图像沿父车方向排布;artic part 不算 IsEngine/IsWagon,
  遍历真车(GetNextUnit/train.h)时被跳过,不能独立成为 First()/段头。
- 提议的"假爸爸身份":纯逻辑角色标记,不是物理父车。
  类比先例——段(segment)两端都带"假引擎"身份(记忆 51063123:
  升级段时 first 与 last 都升为假引擎,使逐段反转后新旧段头都能被标注),
  本设想是把同一"身份随端迁移"思想下放到 artic 组内部的父子层。

## 3. 为什么现有实现会断(背景)

- R3RFlipChainBySegments(段序保持)对段内做"artic 块倒序重链":
  对单 artic 块的段(用户现场一段 = 一列三节铰接车)无法内部调头,
  父车必须先于儿子是焊死不变式 → 就地镜像后 artic 组整体仍停原位,
  却要按新链序(倒序)互相连接 → 出现 idx8(y=515)↔idx3(y=533)
  这种"跨整列车底的断口"(gap=16 > 8),FOLDCHK 判折叠。
- 根因:artic 组是延伸物体,空间朝向不能就地改变;
  而普通散车每节独立,就地反转不产生断口。

## 4. 设想的承诺

让"artic 组的逻辑头/前方向"由"爸爸身份落在哪一端"定义,
而不是由"物理父车在哪一端"定义:
- 平时:真爸爸(父车端)持身份 → artic 组逻辑头 = 物理头。
- first/last 反转时:身份迁移到原尾 part 端(假爸爸升格、原父车降格),
  物理上 artic 组什么都不动(位置/方向/图像/链序全不变)。
- 这样段内 artic 组的"逻辑换向"不再需要移动任何车。

## 5. 关键坑与待核(必须解决才可落地)

1. FOLDCHK 只看物理位置与 direction,不看身份。
   若翻转后 artic 组仍原位,合并链相邻对几何不变 → 折叠判定结果不变。
   只改身份标记不会消除 idx8↔idx3 断口 —— 那个断口是"位置矛盾",
   不是"身份矛盾"。要让本方案起效,必须把它接进真正的判定
   (拼接端点选择、段头/★归属、方向统一判定),否则是无效修改。
2. artic part 不能成为 First()/独立单元(遍历 GetNextUnit 跳过、
   无 orders/不能当段头)。若"新爸爸"仍是 artic part,而链要把该组
   当可拼接的段头用,SetNext/First()/货物/拼接语义都会出问题。
   需明确:新爸爸只是"逻辑组头/拼接面/段头归属"的角色,
   artic 物理与渲染仍由原父车承担(图像不变 = 渲染跟随原父车)。
3. "图像不变"的边界要定义:direction 翻转会让整列 artic 图像
   反向(日志 idx8/idx3 翻转后 dir=7 可见);图像不变指的是
   artic part 间相对图像/排列不变,而不是 direction 也不翻?
   需与用户确认。
4. 若 artic 组真需要空间换向(尾对机车),物理上只有两条路:
   (a) 整组移动/旋转(ReverseTrainSwapVehicles 对 artic 是否安全,待查);
   (b) 接受"逻辑爸爸身份"只在链序/方向统一层生效,
       空间朝向交给行驶掉头(绕轨道)。

## 6. 一句话结论

方向正确、有先例(段双端假引擎身份),但"身份迁移"只能解决
"逻辑头归属/方向统一"层的问题;idx8↔idx3 的位置断口必须靠
物理手段或判定层改造(端点角色统一 / artic 组边界折叠判定)消除,
二者需要配合,不能互相替代。

## 7. 关联

- 记忆 51063123(段双端假引擎身份先例)
- 记忆 41007633 / 97996690(段级逻辑翻转语义)
- R3R_couple_fold_catchup.md / R3R_artic_couple_memo.md(idx8↔idx3 根因)
